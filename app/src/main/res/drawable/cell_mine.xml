<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo rojo para representar la mina -->
    <solid android:color="@color/mine_background" />

    <!-- Bordes redondeados para consistencia -->
    <corners android:radius="1dp" />

    <!-- Borde oscuro para resaltar -->
    <stroke
        android:width="1dp"
        android:color="@color/mine_border" />
</shape>
